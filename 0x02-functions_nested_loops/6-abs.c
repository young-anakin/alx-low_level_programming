#include "main.h"
/**
 * _abs - Return absolute value of a number
 * @n: char type letter
 * Return: absolute value of the number
 */
int _abs(int x)
{
	if (x >=0)
	{
		return (x);
	}
	else {
		return (-x);
	}
}
